<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

	<!-- Define property at the top -->
	<Properties>
		<!-- Use catalina.base if available; otherwise, current directory -->
		<Property name="log.path">${sys:catalina.base:-.}/logs</Property>
	</Properties>

	<Appenders>
		<!-- Console output -->
		<Console name="ConsoleAppender" target="SYSTEM_OUT">
			<PatternLayout
				pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n" />
		</Console>

		<!-- File output -->
		<File name="FileAppender"
			fileName="${log.path}/payroll_report.log" append="true">
			<PatternLayout
				pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n" />
		</File>
	</Appenders>

	<Loggers>
		<!-- Project-level logger -->
		<Logger name="com.payroll.report" level="debug"
			additivity="false">
			<AppenderRef ref="ConsoleAppender" />
			<AppenderRef ref="FileAppender" />
		</Logger>

		<!-- Root logger -->
		<Root level="info">
			<AppenderRef ref="ConsoleAppender" />
			<AppenderRef ref="FileAppender" />
		</Root>
	</Loggers>

</Configuration>
